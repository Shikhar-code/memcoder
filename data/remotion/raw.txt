[
  {
    "task": "Basic Remotion setup failing with React version mismatch",
    "files": [
      "package.json"
    ],
    "summary": "Remotion requires specific React versions, and a mismatch caused the render to fail.",
    "solution": "Align the React version in package.json with the supported Remotion version and run npm install."
  },
  {
    "task": "Video has no audio during preview",
    "files": [
      "composition.tsx"
    ],
    "summary": "Audio track was not included in the Composition component.",
    "solution": "Import and use the <Audio /> component from remotion inside the main sequence."
  },
  {
    "task": "Text animation is jittery",
    "files": [
      "scene1.tsx"
    ],
    "summary": "Using integer frame values for transform instead of floating-point interpolation.",
    "solution": "Use spring() or interpolate() functions provided by remotion to smoothly transition values between frames."
  },
  {
    "task": "Images not loading in headless render",
    "files": [
      "logo.tsx",
      "render.ts"
    ],
    "summary": "Local images were referenced with relative paths that don't resolve correctly during headless render.",
    "solution": "Use staticFile() from remotion to correctly reference assets from the public folder."
  },
  {
    "task": "Composition frame rate mismatch",
    "files": [
      "Root.tsx"
    ],
    "summary": "The useCurrentFrame hook returned values that did not align with the desired 60fps output.",
    "solution": "Ensure the fps prop in the Composition component is set to 60 and calculate all frame math based on that constant."
  },
  {
    "task": "Data fetching blocks video render",
    "files": [
      "data.ts",
      "composition.tsx"
    ],
    "summary": "Async data fetching in components caused the render to capture empty frames before data loaded.",
    "solution": "Use continueRender and delayRender from remotion to pause the rendering process until async fetch operations complete."
  },
  {
    "task": "Dynamic video duration not updating",
    "files": [
      "Root.tsx"
    ],
    "summary": "Passing dynamic props to Composition did not correctly update the durationInFrames prop.",
    "solution": "Use calculateMetadata to dynamically compute durationInFrames based on the fetched data before the composition mounts."
  },
  {
    "task": "Complex transitions dropping frames",
    "files": [
      "transition.tsx"
    ],
    "summary": "Heavy CSS filters were used for transitions, causing performance issues in the browser.",
    "solution": "Replace CSS filters with optimized WebGL shaders or simplified opacity/transform animations for smoother rendering."
  },
  {
    "task": "Audio multi-track synchronization drifting",
    "files": [
      "podcast.tsx"
    ],
    "summary": "Multiple audio tracks started overlapping and drifting out of sync during long exports.",
    "solution": "Use Sequence components to strictly bound the start and end times of each audio clip, locking them to specific frames."
  },
  {
    "task": "Stale data in parameterized renders",
    "files": [
      "render.ts"
    ],
    "summary": "Input props passed to renderMedia were cached aggressively by the bundler.",
    "solution": "Pass a unique cache-busting key or use the --gl prop to ensure the render uses fresh input props each time."
  },
  {
    "task": "Parallel rendering out of memory",
    "files": [
      "lambda.ts"
    ],
    "summary": "Remotion Lambda spawned too many concurrent chunks, exceeding the 2GB memory limit on AWS.",
    "solution": "Reduce the concurrencyPerLambda or increase the Lambda memory allocation to 4GB to handle heavy WebGL scenes."
  },
  {
    "task": "Custom WebGL shader crashes on export",
    "files": [
      "glsl_comp.tsx"
    ],
    "summary": "A Three.js canvas threw a context loss error during headless rendering.",
    "solution": "Enable preserveDrawingBuffer in the WebGLRenderer and handle context restoration gracefully in the useEffect hook."
  },
  {
    "task": "Custom font not loading in Lambda",
    "files": [
      "font.ts"
    ],
    "summary": "Custom woff2 fonts did not load in the AWS Lambda environment because of strict CORS/MIME types.",
    "solution": "Host the font files on S3 with correct CORS headers and inject them globally before the render starts using delayRender."
  },
  {
    "task": "Handling massive video source data",
    "files": [
      "large_video.tsx"
    ],
    "summary": "Attempting to load a massive source video caused the headless browser to crash.",
    "solution": "Pre-process the 10GB video into smaller HTTP Live Streaming (HLS) chunks and use the <OffthreadVideo /> component."
  },
  {
    "task": "Frame-perfect subtitle alignment",
    "files": [
      "subtitles.tsx"
    ],
    "summary": "Subtitles parsed from SRT were occasionally 1 frame off due to rounding errors.",
    "solution": "Parse timestamps into precise frame indices using Math.round(timeInSeconds * fps) rather than relying on browser timing."
  },
  {
    "task": "Monolithic Remotion project slow to bundle",
    "files": [
      "webpack.config.ts"
    ],
    "summary": "A single project contained 50 compositions, causing massive webpack build times before render.",
    "solution": "Split compositions into separate entry points and use dynamic imports to isolate build dependencies."
  },
  {
    "task": "State management across deeply nested sequences",
    "files": [
      "store.ts",
      "scene.tsx"
    ],
    "summary": "Prop drilling frame calculations became unmaintainable across nested components.",
    "solution": "Implement a React Context specifically for animation timeline state, storing global frame offsets and easing configurations."
  },
  {
    "task": "Designing a generic render pipeline",
    "files": [
      "pipeline.ts"
    ],
    "summary": "Hardcoded render scripts were failing when trying to scale to multiple diverse video types.",
    "solution": "Create an abstract render queue system using BullMQ that dynamically assigns inputProps and composition IDs to workers."
  },
  {
    "task": "CI/CD integration for video previews",
    "files": [
      "github_actions.yml"
    ],
    "summary": "PRs needed automated video previews, but GitHub actions lacked necessary rendering dependencies.",
    "solution": "Use the official remotion-github-actions setup to render short low-res previews of compositions and post them as PR comments."
  },
  {
    "task": "Scaling to 10k video renders per day",
    "files": [
      "infra.ts",
      "aws_cdk.ts"
    ],
    "summary": "Local EC2 instances were bottlenecked handling the extensive video generation queue.",
    "solution": "Migrate completely to Remotion Lambda, deploying to multiple regions and using an SQS event-driven architecture to distribute load."
  }
]
