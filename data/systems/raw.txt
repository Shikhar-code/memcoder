[
  {
    "task": "Permission denied executing script",
    "files": [
      "script.sh"
    ],
    "summary": "Attempting to run a bash script resulted in a 'permission denied' error from the shell.",
    "solution": "Run chmod +x script.sh to grant execute permissions to the file owner."
  },
  {
    "task": "Port already in use",
    "files": [
      "server.py"
    ],
    "summary": "The application failed to start because the port was bound by another process.",
    "solution": "Find the blocking process using netstat or lsof and terminate it using the kill command."
  },
  {
    "task": "Environment variables not found",
    "files": [
      "main.py"
    ],
    "summary": "A script failed because it could not find required API keys in the environment.",
    "solution": "Export the variables in the ~/.bashrc file or pass them directly when invoking the script."
  },
  {
    "task": "Makefile relinking everything",
    "files": [
      "Makefile"
    ],
    "summary": "Running 'make' rebuilt all files even when no source code had changed.",
    "solution": "Fix the target dependency lists in the Makefile to explicitly include the correct source files as prerequisites."
  },
  {
    "task": "Cron job failing silently",
    "files": [
      "crontab"
    ],
    "summary": "A scheduled backup job failed, but there were no errors printed to the console.",
    "solution": "Redirect standard output and stderr to a log file within the crontab entry."
  },
  {
    "task": "Zombie processes accumulating",
    "files": [
      "daemon.c"
    ],
    "summary": "The system process table filled up with 'defunct' child processes that had terminated.",
    "solution": "Ensure the parent process correctly calls wait() or waitpid() to reap child processes after they exit."
  },
  {
    "task": "Thread race condition",
    "files": [
      "worker.c"
    ],
    "summary": "Multiple threads updating a shared counter resulted in an incorrect final tally.",
    "solution": "Protect the shared state by wrapping the critical section with a mutex lock or using atomic operations."
  },
  {
    "task": "Segfault due to double free",
    "files": [
      "memory.c"
    ],
    "summary": "The program crashed with a segmentation fault because it attempted to free the same pointer twice.",
    "solution": "Set the pointer to NULL immediately after freeing it, or migrate to smart pointers."
  },
  {
    "task": "Buffer overflow vulnerability",
    "files": [
      "parser.c"
    ],
    "summary": "Reading a long string into a fixed-size char array corrupted adjacent memory.",
    "solution": "Replace unsafe functions like strcpy and sprintf with their bounds-checked alternatives strncpy and snprintf."
  },
  {
    "task": "Sockets blocking indefinitely",
    "files": [
      "server.c"
    ],
    "summary": "A single slow client connection caused the entire server to freeze waiting for data.",
    "solution": "Set the socket to non-blocking mode using fcntl and multiplex connections using select or epoll."
  },
  {
    "task": "Memory fragmentation over time",
    "files": [
      "allocator.c"
    ],
    "summary": "Long-running processes suffered from degraded performance and high memory usage due to heap fragmentation.",
    "solution": "Implement a custom memory pool allocator tailored to the specific object sizes the application uses."
  },
  {
    "task": "Lock contention slowing multi-threading",
    "files": [
      "queue.cpp"
    ],
    "summary": "Adding more CPU cores decreased performance because threads spent time waiting on a single mutex.",
    "solution": "Replace the mutex-based data structure with a lock-free queue utilizing atomic compare-and-swap operations."
  },
  {
    "task": "Kernel panic in custom driver",
    "files": [
      "driver.c"
    ],
    "summary": "The operating system completely crashed due to a bug in a custom PCIe driver.",
    "solution": "Fix a null pointer dereference in the hardware interrupt handler where memory cannot be paged."
  },
  {
    "task": "High context switch overhead",
    "files": [
      "trading.cpp"
    ],
    "summary": "A low-latency application was slowed down by the OS scheduler moving threads between cores.",
    "solution": "Pin critical threads to specific CPU cores using thread affinity to maintain L1 cache warmth."
  },
  {
    "task": "Network packet drops at high load",
    "files": [
      "network.c"
    ],
    "summary": "The server began dropping UDP packets when receiving massive packets per second.",
    "solution": "Tune the NIC ring buffers and implement zero-copy networking using the sendfile system call."
  },
  {
    "task": "Tight coupling in microservices",
    "files": [
      "architecture.md"
    ],
    "summary": "Changes in one service broke others because they relied on brittle JSON schemas.",
    "solution": "Introduce gRPC and Protocol Buffers to enforce strict, backward-compatible interface contracts between services."
  },
  {
    "task": "Unreliable asynchronous workflows",
    "files": [
      "jobs.py"
    ],
    "summary": "Background tasks triggered via HTTP webhooks were lost when the receiving server crashed.",
    "solution": "Use a durable message broker like RabbitMQ or Kafka to queue tasks, ensuring they are not acknowledged until processed."
  },
  {
    "task": "Split brain in distributed cluster",
    "files": [
      "cluster.conf"
    ],
    "summary": "A network partition caused two nodes to both think they were the primary master.",
    "solution": "Implement a consensus algorithm like Raft or use Zookeeper to strictly manage leader election."
  },
  {
    "task": "Hard to trace requests",
    "files": [
      "tracing.py"
    ],
    "summary": "Debugging latency across different microservices was impossible relying only on text logs.",
    "solution": "Implement distributed tracing using OpenTelemetry, propagating trace IDs through HTTP headers."
  },
  {
    "task": "Single point of failure in load balancer",
    "files": [
      "haproxy.cfg"
    ],
    "summary": "The entire platform went offline when the single load balancer server had a hardware failure.",
    "solution": "Setup HAProxy on two separate servers and use keepalived with a floating VIP for automatic failover."
  }
]
