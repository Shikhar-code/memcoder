[
  {
    "task": "Unable to connect to database",
    "files": [
      "config.py"
    ],
    "summary": "The application threw a connection timeout error on startup.",
    "solution": "Check the host, port, and credentials in the connection string and ensure firewall rules allow traffic."
  },
  {
    "task": "Query returning duplicate rows",
    "files": [
      "queries.sql"
    ],
    "summary": "A simple join resulted in multiple identical rows being returned for each user.",
    "solution": "Use the DISTINCT keyword or fix the JOIN conditions to ensure a one-to-one relationship match."
  },
  {
    "task": "Cannot insert null into column",
    "files": [
      "schema.sql"
    ],
    "summary": "An insert statement failed because a field was missing in the payload.",
    "solution": "Update the table schema to ALLOW NULL for that column or provide a sensible DEFAULT value."
  },
  {
    "task": "Missing foreign key constraint",
    "files": [
      "schema.sql"
    ],
    "summary": "Rows were being deleted from the parent table, leaving orphaned records in the child table.",
    "solution": "Create the child table with the correct REFERENCES keyword and specify ON DELETE CASCADE."
  },
  {
    "task": "Slow SELECT query on large table",
    "files": [
      "queries.sql"
    ],
    "summary": "Filtering users by email took several seconds on a table with massive rows.",
    "solution": "Add a B-Tree index to the email column to optimize the lookup speed."
  },
  {
    "task": "N+1 query problem in ORM",
    "files": [
      "app.py"
    ],
    "summary": "Loading a list of posts and their authors triggered numerous separate database queries.",
    "solution": "Use eager loading (like .includes() or .joinedload() depending on the ORM) to fetch all authors in a single query."
  },
  {
    "task": "Deadlock during concurrent updates",
    "files": [
      "transactions.sql"
    ],
    "summary": "Two concurrent transactions modifying the same two tables got stuck waiting for each other.",
    "solution": "Ensure all application transactions acquire locks on tables in the exact same deterministic order."
  },
  {
    "task": "Schema migration locks table",
    "files": [
      "migration.sql"
    ],
    "summary": "Adding a column to a massive table locked it for minutes, causing production downtime.",
    "solution": "Use concurrent index creation and online DDL operations supported by the database engine to avoid table locks."
  },
  {
    "task": "Connection pool exhaustion",
    "files": [
      "pool.py"
    ],
    "summary": "The app stopped responding because all available database connections were checked out.",
    "solution": "Increase the connection pool size and ensure code properly releases connections back to the pool in a finally block."
  },
  {
    "task": "Inefficient pagination performance",
    "files": [
      "queries.sql"
    ],
    "summary": "Querying deep pages using OFFSET took over 10 seconds because the database scanned all previous rows.",
    "solution": "Switch from OFFSET/LIMIT pagination to cursor-based pagination using a WHERE id > clause."
  },
  {
    "task": "High replication lag",
    "files": [
      "config.conf"
    ],
    "summary": "Read replicas were serving stale data that was several minutes behind the primary database.",
    "solution": "Optimize heavy write queries on the primary and ensure the replica hardware matches the primary's specifications."
  },
  {
    "task": "Query planner ignoring index",
    "files": [
      "queries.sql"
    ],
    "summary": "The database performed a full table scan despite an index existing on the queried column.",
    "solution": "Update table statistics using the ANALYZE command so the query planner makes accurate cost estimations."
  },
  {
    "task": "Unpredictable JSONB query performance",
    "files": [
      "schema.sql"
    ],
    "summary": "Filtering based on keys inside a JSON column caused massive CPU spikes.",
    "solution": "Create GIN (Generalized Inverted Index) indexes targeting the specific frequently queried JSON paths."
  },
  {
    "task": "Sharding key hotspotting",
    "files": [
      "shard_config.py"
    ],
    "summary": "A sequential ID sharding key caused all new writes to hit a single database shard, overloading it.",
    "solution": "Choose a hashing strategy for the sharding key to evenly distribute writes across all available shards."
  },
  {
    "task": "Write amplification in SSD",
    "files": [
      "my.cnf"
    ],
    "summary": "The database was burning through SSD lifespan due to excessive small write operations.",
    "solution": "Tune the InnoDB log file size and flush behavior to batch writes more efficiently."
  },
  {
    "task": "Relational database bottleneck",
    "files": [
      "architecture.md"
    ],
    "summary": "Storing and querying millions of short-lived session tokens overloaded the primary database.",
    "solution": "Migrate session, cache, and ephemeral data to a fast, in-memory data store like Redis."
  },
  {
    "task": "Complex reporting queries slowing main DB",
    "files": [
      "infra.tf"
    ],
    "summary": "Running analytical aggregations caused transaction timeouts for normal user operations.",
    "solution": "Implement a read replica dedicated solely to analytical queries, keeping the primary free for traffic."
  },
  {
    "task": "Audit log table growing too large",
    "files": [
      "schema.sql"
    ],
    "summary": "An audit table grew too large, making backups slow and queries impossible.",
    "solution": "Move to an Event Sourcing architecture or partition the audit table, archiving old partitions to cold storage."
  },
  {
    "task": "Searching text fields is slow",
    "files": [
      "search.py"
    ],
    "summary": "Using LIKE queries for text search resulted in full table scans every time.",
    "solution": "Integrate Elasticsearch for full-text search capabilities and sync data continuously via CDC."
  },
  {
    "task": "Cross-service data consistency",
    "files": [
      "saga.py"
    ],
    "summary": "A failure in the payment service left the order service in an inconsistent state.",
    "solution": "Implement the Saga pattern using an orchestrator to manage distributed transactions and execute compensating actions."
  }
]
